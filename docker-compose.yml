services:
  icloud-drive-mcp:
    build: .
    image: icloud-drive-mcp:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      # Keeps the Apple session across restarts.
      - icloud-data:/data

volumes:
  icloud-data:
